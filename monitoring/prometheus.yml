global:
  scrape_interval: 5s

scrape_configs:

  - job_name: "prometheus"
    static_configs:
      - targets: ["prometheus:9090"]

  - job_name: "prompt-firewall-api"
    metrics_path: /metrics
    static_configs:
      - targets: ["prompt-firewall-api:8000"]